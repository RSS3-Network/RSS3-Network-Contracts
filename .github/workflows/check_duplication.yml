name: Check code duplication

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  duplicate-code-check:
    name: Check for duplicate code
    runs-on: ubuntu-20.04
    steps:
      - name: Check for duplicate code
        uses: platisd/duplicate-code-detection-tool@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          file_extensions: "sol"
          # Only report similarities above 5%
          ignore_below: 10
          # If a file is more than 60% similar to another, then the job fails
          fail_above: 60
          # If a file is more than 30% similar to another, show a warning symbol in the report
          warn_above: 30
          only_code: true
           # Leave only one comment with the report and update it for consecutive runs
          one_comment: true
          # The message to be displayed at the start of the report
          header_message_start: "The following files have a similarity above the threshold:"
